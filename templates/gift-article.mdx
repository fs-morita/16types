---
# 記事テンプレート（ギフト層）
#
# これは src/content/posts/ の外に置いてあるので、ビルド対象になりません。
# 新しい記事を書くときは、このファイルを src/content/posts/<slug>.mdx にコピーして使います。
#
# frontmatter は src/content.config.ts の zod スキーマで検証されます。
# types に存在しないコードを書くと、静かに壊れるのではなくビルドが失敗します。

title: 【例】ENFPへのプレゼント。ふらっと使える、決めつけないもの7選
description: 広報運動家（ENFP）に贈るなら。外向・直観・感情・知覚の4指標から、選ぶ基準を分解して考えます。
pubDate: 2026-01-01
types: ['ENFP']
intent: gift          # explainer | self | gift
occasion: 誕生日       # 誕生日 | クリスマス | バレンタイン | 母の日 | 送別 | 通年
draft: true           # 公開するときに false にする
---

import ProductCard from '../../components/ProductCard.astro';
import ComparisonTable from '../../components/ComparisonTable.astro';

{/* 1. 読者がいま困っている場面から入る。商品の話は後。 */}

{/* 2. この記事が向いていない人を先に外す。信頼はここで作る。 */}

## 選ぶときの基準

{/* 3. タイプ名ではなく指標に翻訳して書く。
       「ENFPだからこれ」ではなく「使い方が固定されない人だから、これ」。
       各タイプの指標と対応する指針は /[type]/ のハブページに表で出ています。 */}

## 比較表

<ComparisonTable
  headers={['商品', '価格帯', '向いている場面', '注意点']}
  rows={[
    ['商品A', '¥5,000前後', '…', '…'],
    ['商品B', '¥8,000前後', '…', '…'],
  ]}
/>

## それぞれの詳細

{/*
  商品は src/data/products.ts のカタログを id で参照します。
  商品名・ASIN・「それが何か」はカタログ側に一度だけ書きます。
  同じ商品が複数タイプの記事に載るため、直書きすると変更のたびに全記事を直す羽目になります。

  存在しない id を書くとビルドが落ちます（黙って商品カードが消えるより良いため）。
  id の一覧は products.csv、または `npm run products:check` の出力で確認できます。

  商品のスペック説明はここに書きません。記事の中身は上の性格分析の側にあります。
*/}

<ProductCard id="board-game" />
<ProductCard id="instant-camera" />

## 低評価レビューで指摘が集中していた点

{/* 4. ここが最も読まれる。正直に書く。
       他サイトがほとんど書かないので、差がつくのもここ。 */}

## 使ってみた実感

{/*
  5. この節は、実際に使った商品がある場合だけ書く。
     使っていない商品に「使ってみた」を書かないこと。
     景表法（優良誤認）のリスクがあるため。
     使っていない場合はこの節ごと削除し、公開情報の整理として書く。
*/}

## まとめ
